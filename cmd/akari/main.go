// Command akari is the session-backup client daemon.
//
// The client is implemented in a later milestone (discovery, git remote
// resolution, and the upload protocol). This stub exists so the module builds
// and the command is wired up.
package main

import "fmt"

func main() {
	fmt.Println("akari client: not yet implemented (see DESIGN.md, milestone 3)")
}
