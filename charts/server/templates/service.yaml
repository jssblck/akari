apiVersion: v1
kind: Service
metadata:
  name: {{ include "akari.name" . }}
  labels:
    {{- include "akari.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "akari.selectorLabels" . | nindent 4 }}
